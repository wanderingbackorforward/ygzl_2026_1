最小运行步骤：

1. 安装依赖：pip install -r repo_src/requirements.txt
2. 运行：python repo_src/start_system.py

说明：

- 仅包含必要源码与前端脚本，构建产物与大资源未纳入
- 若依赖路径需要调整，请将 repo_src 加入 PYTHONPATH 或在启动脚本中相对导入

回滚与再复制：

- 原始目录未改动，如需恢复可直接使用 copy_repo_src.py 重新复制
