## 整体目标
- 对照老代码恢复工单页完整渲染与样式
- 修复“沉降/总览”页面的渲染缺失，保证图表与3D总览正常
- 启动默认落地页切换为封面 cover.html

## 工单页（tickets.html/tickets.js/style）
- 引入并对齐老版样式：确保加载 css/style.css 与（若存在）css/layout.css，检查颜色、卡片、表格、分页等样式类名一致
- 对齐 DOM 结构与脚本期望的 ID：补齐 messageArea、ticketList、pagination、ticketForm、statusFilter、typeFilter、priorityFilter 等容器
- 复用老代码 UI 布局：在 tickets.html 中加入筛选区、统计区、列表区、分页和创建表单的结构，并匹配 tickets.js 绑定事件
- 后端接口核验：确认 /api/tickets、/api/tickets/statistics、/api/tickets/config/* 已注册并返回数据（当前已注册），前端只需渲染
- 验证：打开 /static/tickets.html，确认筛选、分页、创建工单均可用

## 沉降/总览页（index.html、charts.js、overview.html/overview_enhanced.js）
- 首页 index.html：导航统一为 compact-nav，保留老版面板与图表容器 ID（trend-chart/points-chart/time-series-chart/rate-chart/point-details），确保 charts.js 初始化无空指针
- charts.js：验证 initCyberpunkTheme、loadSummaryData、setupPointSelector、setupChartZoom 逻辑都能找到对应元素；如有空元素，补齐 HTML 或加判空
- 总览 overview.html：对齐老版结构，补齐 loading-indicator、view-controls、viewer-canvas 等元素；保持 three.js/GLTFLoader/OrbitControls 引入顺序
- GLB加载：使用 /static/glb/ 路由，模型路径 glb/004-051.glb（已验证可加载大文件）
- 验证：/static/overview.html 显示3D模型与控制组件；首页 / 渲染出图表与面板

## 启动默认页面（index 路由与启动脚本）
- 后端路由：将 @app.route('/') 改为重定向/渲染封面（/static/cover.html），保持其他页面从封面进入
- 启动脚本：start_system.py 打开浏览器的 URL 改为 http://localhost:5000/static/cover.html
- 验证：重启后直接落地封面页，导航正常

## 细节与回退
- 保留现有蓝图注册（crack/temperature/vibration/ticket）与 MySQL 查询逻辑
- 如存在浏览器字体阻拦（Rajdhani）导致控制台噪音，后续可切本地字体（不影响功能）

## 交付与验证
- 逐文件更新：repo_src/static/tickets.html、repo_src/static/js/tickets.js（仅必要判空与事件绑定修正）、repo_src/templates/index.html、repo_src/static/overview.html、repo_src/static/js/overview_enhanced.js、repo_src/modules/api/api_server.py（index重定向）、repo_src/start_system.py（启动URL）
- 打开 /static/tickets.html、/static/overview.html、/ 检查实际渲染与交互

请确认是否按上述方案实施，我将一次性完成修改并重启验证。