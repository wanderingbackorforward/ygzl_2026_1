[project]
name = "terrain-settlement-api"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "Flask",
  "Flask-Cors",
  "Werkzeug",
  "requests",
  "numpy",
  "pandas",
  "SQLAlchemy",
  "pyshp",
  "scikit-learn",
  "statsmodels",
  "scipy",
]

[tool.uv]
package = false
